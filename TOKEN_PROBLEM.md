# ⚠️ Проблема з токеном

Ваш токен не працює. Можливі причини:
- Токен скопійовано неправильно
- Токен вже відкликано
- Токен не має потрібних прав

## 🔧 Рішення: Створіть новий токен

### Крок 1: Відкрийте налаштування токенів
**Пряме посилання:** https://github.com/settings/tokens

### Крок 2: Відкличіть старий токен (якщо є)
1. Знайдіть старий токен в списку
2. Натисніть "Revoke" (Відкликати)

### Крок 3: Створіть новий токен
1. Натисніть **"Generate new token"** → **"Generate new token (classic)"**
2. **Note:** `rust_bot_backup`
3. **Expiration:** `No expiration`
4. **Select scopes:** ✅ **`repo`** (обов'язково!)
5. Натисніть **"Generate token"**
6. **Скопіюйте токен одразу!**

### Крок 4: Використайте новий токен

```bash
cd /home/konko/rust_bot

# Замініть НОВИЙ_ТОКЕН на ваш новий токен
git remote set-url origin https://sopko-dev:НОВИЙ_ТОКЕН@github.com/sopko-dev/telegram_bot.git
git push origin main
```

## 🚀 Альтернатива: Використайте GitHub CLI

Якщо не хочете створювати токен:

```bash
# Авторизуйтеся (відкриється браузер)
gh auth login

# Завантажте код
cd /home/konko/rust_bot
git push origin main
```

---

**Важливо:** Переконайтеся, що при створенні токена ви обираєте право **`repo`** (повний доступ до репозиторіїв)!
