# 🔑 Правильні права для GitHub Personal Access Token

## ⚠️ Важливо: GitHub змінив права!

GitHub змінив назви прав для токенів. Замість `repo` тепер використовуються інші назви.

## 📋 Які права обирати:

### Для класичних токенів (Tokens classic):

При створенні токена оберіть:

1. **`repo`** - Repository access
   - ✅ **Full control of private repositories** (Повний контроль над приватним репозиторіями)
   - Це найважливіше право!

Або окремо:
- ✅ **`repo:status`** - Access commit status
- ✅ **`repo_deployment`** - Access deployment status  
- ✅ **`public_repo`** - Access public repositories
- ✅ **`repo:invite`** - Access repository invitations
- ✅ **`security_events`** - Access security events

**Але найпростіше - оберіть `repo` (повний контроль)!**

### Для fine-grained tokens (нові токени):

Якщо використовуєте новий тип токенів:
- **Repository permissions** → **Contents** → **Read and write**
- **Repository permissions** → **Metadata** → **Read-only** (або Read and write)

## 🎯 Покрокова інструкція:

### Крок 1: Відкрийте створення токена
https://github.com/settings/tokens

### Крок 2: Оберіть тип токена

**Рекомендовано: "Tokens (classic)"** - простіше налаштувати

### Крок 3: Налаштування

1. **Note:** `rust_bot_backup`
2. **Expiration:** `No expiration` (або встановіть термін)
3. **Select scopes:** Прокрутіть вниз до секції **"repo"**

### Крок 4: Оберіть права

Знайдіть секцію **"repo"** і встановіть:
- ✅ **`repo`** - Full control of private repositories

**АБО** якщо немає опції `repo`, оберіть:
- ✅ **`public_repo`** - Access public repositories (якщо репозиторій публічний)
- ✅ **`repo:status`** - Access commit status
- ✅ **`repo_deployment`** - Access deployment status

### Крок 5: Згенеруйте токен

1. Прокрутіть вниз
2. Натисніть **"Generate token"**
3. **Скопіюйте токен одразу!**

## 🔍 Якщо не бачите права "repo":

### Варіант 1: Прокрутіть вниз
Права можуть бути внизу сторінки. Прокрутіть до секції "repo".

### Варіант 2: Використайте пошук
Натисніть `Ctrl+F` (або `Cmd+F` на Mac) і шукайте "repo".

### Варіант 3: Використайте fine-grained token
1. Натисніть "Generate new token" → **"Generate new token (fine-grained)"**
2. Оберіть репозиторій: `sopko-dev/telegram_bot`
3. **Repository permissions:**
   - **Contents:** Read and write
   - **Metadata:** Read-only

## ✅ Перевірка прав токена:

Після створення токена перевірте:
1. Відкрийте: https://github.com/settings/tokens
2. Знайдіть ваш токен
3. Перевірте, чи є право `repo` або `public_repo`

## 🚀 Використання токена:

```bash
cd /home/konko/rust_bot

# Замініть НОВИЙ_ТОКЕН на ваш токен
git remote set-url origin https://sopko-dev:НОВИЙ_ТОКЕН@github.com/sopko-dev/telegram_bot.git
git push origin main
```

---

**Якщо все ще не працює:** Використайте GitHub CLI - це простіше:
```bash
gh auth login
git push origin main
```
