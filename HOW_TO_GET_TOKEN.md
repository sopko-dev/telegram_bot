# 🔑 Як отримати Personal Access Token для GitHub

## 📍 Де знайти токен:

### Крок 1: Відкрийте налаштування токенів

**Пряме посилання:** https://github.com/settings/tokens

Або вручну:
1. Відкрийте https://github.com
2. Натисніть на ваш аватар (правый верхній кут)
3. Оберіть **"Settings"** (Налаштування)
4. В лівому меню знайдіть **"Developer settings"** (внизу)
5. Натисніть **"Personal access tokens"**
6. Оберіть **"Tokens (classic)"**

### Крок 2: Створіть новий токен

1. Натисніть кнопку **"Generate new token"**
2. Оберіть **"Generate new token (classic)"**

### Крок 3: Налаштуйте токен

1. **Note** (Нотатка): Введіть назву, наприклад:
   - `rust_bot_backup`
   - `telegram_bot_github`
   - `backup_token`

2. **Expiration** (Термін дії): Оберіть:
   - `No expiration` (Без терміну) - для постійного використання
   - Або встановіть дату (наприклад, 90 днів)

3. **Select scopes** (Оберіть права): Встановіть галочку:
   - ✅ **`repo`** - Повний контроль над приватним репозиторіями
     - Це дає права на читання та запис

4. **Не встановлюйте інші галочки** (якщо не потрібно)

### Крок 4: Згенеруйте та скопіюйте токен

1. Прокрутіть вниз
2. Натисніть кнопку **"Generate token"** (зелена кнопка внизу)
3. **ВАЖЛИВО:** Токен показується тільки один раз!
4. **Скопіюйте токен одразу!** Він виглядає так:
   ```
   ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   ```
5. Збережіть токен в безпечному місці

### Крок 5: Використайте токен

```bash
cd /home/konko/rust_bot

# Замініть ВАШ_ТОКЕН на скопійований токен
git push https://ВАШ_ТОКЕН@github.com/sopko-dev/telegram_bot.git main
```

**Приклад:**
```bash
git push https://ghp_abc123xyz456@github.com/sopko-dev/telegram_bot.git main
```

---

## 🎯 Швидкий спосіб (покроково):

1. **Відкрийте:** https://github.com/settings/tokens
2. **Натисніть:** "Generate new token" → "Generate new token (classic)"
3. **Назва:** `rust_bot_backup`
4. **Термін:** `No expiration`
5. **Права:** ✅ `repo`
6. **Натисніть:** "Generate token"
7. **Скопіюйте токен**
8. **Використайте:**
   ```bash
   cd /home/konko/rust_bot
   git push https://ВАШ_ТОКЕН@github.com/sopko-dev/telegram_bot.git main
   ```

---

## ⚠️ Важливо:

- ❌ **НЕ публікуйте токен** в публічних місцях
- ❌ **НЕ діліться токеном** з іншими
- ✅ **Зберігайте токен** в безпечному місці
- ✅ Якщо токен втрачено - створіть новий

---

## 🔄 Якщо токен втрачено:

1. Відкрийте: https://github.com/settings/tokens
2. Знайдіть старий токен
3. Натисніть "Revoke" (відкликати)
4. Створіть новий токен (як описано вище)

---

## 💡 Альтернатива: Використання GitHub CLI

Якщо не хочете створювати токен, використайте GitHub CLI:

```bash
# Авторизуйтеся (відкриється браузер)
gh auth login

# Завантажте код
cd /home/konko/rust_bot
git push origin main
```

Це простіше, але потребує встановлення GitHub CLI (який вже встановлено).

---

**Готово!** Після отримання токена ви зможете завантажити резервну копію на GitHub! 🚀
