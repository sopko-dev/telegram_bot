# 🚀 Детальна інструкція: Деплой на Railway.app

Railway.app - найпростіший спосіб задеплоїти бота безкоштовно без кредитної картки!

## ✅ Переваги Railway:

- ✅ Повністю безкоштовно (до 500 годин/місяць)
- ✅ Не потрібна кредитна картка
- ✅ Автоматичний деплой з GitHub
- ✅ Автоматичний перезапуск
- ✅ Простий інтерфейс

## 📋 Крок 1: Зареєструйтеся на Railway

1. **Відкрийте:** https://railway.app
2. **Натисніть:** "Login" або "Get Started"
3. **Оберіть:** "Login with GitHub"
4. **Авторизуйтеся** через GitHub (надайте права Railway)

## 📋 Крок 2: Створіть новий проект

1. **Натисніть:** "New Project" (або велика кнопка "+ New")
2. **Оберіть:** "Deploy from GitHub repo"
3. **Знайдіть репозиторій:** `sopko-dev/telegram_bot`
4. **Натисніть** на репозиторій
5. Railway автоматично почне деплой

## 📋 Крок 3: Додайте змінну оточення (ТОКЕН БОТА)

**ВАЖЛИВО:** Без токена бот не працюватиме!

1. В проекті Railway натисніть на **сервіс** (якщо є кілька)
2. Перейдіть в **"Variables"** (або "Settings" → "Variables")
3. Натисніть **"New Variable"**
4. Додайте:
   - **Name:** `TELEGRAM_BOT_TOKEN`
   - **Value:** `ваш_токен_від_BotFather`
5. Натисніть **"Add"**
6. Railway автоматично перезапустить бота з новим токеном

## 📋 Крок 4: Перевірка деплою

1. Перейдіть в **"Deployments"** (або "Logs")
2. Перевірте логи - має бути:
   ```
   Бот запускається...
   Бот готовий до роботи!
   ```
3. Якщо є помилки - перевірте:
   - Чи додано токен `TELEGRAM_BOT_TOKEN`?
   - Чи правильний токен?

## 📋 Крок 5: Тестування бота

1. Відкрийте Telegram
2. Знайдіть вашого бота
3. Надішліть `/start`
4. Бот має відповісти!

## 🔍 Корисні функції Railway

### Перегляд логів:
- В проекті натисніть на сервіс
- Перейдіть в "Logs"
- Бачите всі логи в реальному часі

### Перезапуск:
- В проекті натисніть на сервіс
- Натисніть "Redeploy" (або три крапки → "Redeploy")

### Налаштування:
- "Settings" → "Variables" - змінні оточення
- "Settings" → "Domains" - домени (не потрібно для бота)

## ⚙️ Налаштування проекту

Railway автоматично визначить Rust проект за файлами:
- ✅ `Cargo.toml` - визначить Rust
- ✅ `railway.json` - конфігурація (опціонально)
- ✅ `Procfile` - команда запуску (опціонально)

Якщо Railway не визначить автоматично, в "Settings" → "Build Command":
```
cargo build --release
```

І в "Start Command":
```
./target/release/telegram_bot
```

## 💰 Вартість

- **Безкоштовний план:** 500 годин/місяць
- **Для Telegram бота:** Більш ніж достатньо!
- **Після 500 годин:** Бот призупиниться, але можна перезапустити

## 🐛 Розв'язання проблем

### Проблема: Бот не запускається

**Перевірте:**
1. Чи додано `TELEGRAM_BOT_TOKEN`?
2. Чи правильний токен?
3. Перегляньте логи в Railway

**Рішення:**
- Додайте/оновіть токен в Variables
- Перезапустіть деплой (Redeploy)

### Проблема: "Build failed"

**Перевірте логи:**
- Можливо проблема з компіляцією Rust
- Перевірте, чи всі залежності в `Cargo.toml`

**Рішення:**
- Перевірте `Cargo.toml`
- Спробуйте локально: `cargo build`

### Проблема: Бот не відповідає

**Перевірте:**
1. Логи в Railway - чи бот запущений?
2. Чи правильний токен?
3. Чи бот активний в Telegram?

**Рішення:**
- Перевірте логи
- Перезапустіть бота
- Перевірте токен

## 📊 Моніторинг

Railway показує:
- Статус деплою
- Логи в реальному часі
- Використання ресурсів
- Історію деплоїв

## 🔄 Оновлення бота

Коли ви зміните код:

1. Зробіть commit та push на GitHub:
   ```bash
   git add .
   git commit -m "Оновлення бота"
   git push origin main
   ```

2. Railway автоматично:
   - Визначить зміни
   - Збереже стару версію
   - Скомпілює нову
   - Запустить нову версію

## 🎯 Швидкий старт (підсумок)

1. ✅ Зареєструйтеся: https://railway.app (через GitHub)
2. ✅ New Project → Deploy from GitHub
3. ✅ Оберіть: `sopko-dev/telegram_bot`
4. ✅ Variables → Додайте: `TELEGRAM_BOT_TOKEN` = `ваш_токен`
5. ✅ Готово! Бот працює! 🎉

---

**Питання?** Перевірте логи в Railway або дивіться `FREE_DEPLOYMENT.md`
