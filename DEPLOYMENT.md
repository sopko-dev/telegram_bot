# 🚀 Як запустити бота, щоб він працював завжди

> 💰 **Шукаєте безкоштовний варіант?** Дивіться файл `FREE_DEPLOYMENT.md` для безкоштовних хмарних сервісів!

## ⚠️ Важливо

**Якщо ви вимкнете комп'ютер, бот перестане працювати!**

Бот працює тільки коли:
- Комп'ютер увімкнений
- Бот запущений
- Є інтернет-з'єднання

---

## 📋 Варіанти запуску

### Варіант 1: Systemd Service (рекомендовано для серверів)

Якщо у вас є Linux сервер, який завжди включений, можна налаштувати автозапуск через systemd.

**Переваги:**
- ✅ Автоматично запускається при завантаженні системи
- ✅ Автоматично перезапускається при збоях
- ✅ Легко керувати (start, stop, restart)
- ✅ Логи зберігаються

**Інструкція:**

1. Створіть файл конфігурації:
```bash
sudo nano /etc/systemd/system/telegram-bot.service
```

2. Вставте наступний вміст (замініть `konko` на ваш username та шлях до токена):
```ini
[Unit]
Description=Telegram Bot для українців у Чехії
After=network.target

[Service]
Type=simple
User=konko
WorkingDirectory=/home/konko/rust_bot
Environment="TELEGRAM_BOT_TOKEN=ваш_токен_тут"
ExecStart=/home/konko/.cargo/bin/cargo run
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

3. Замініть `ваш_токен_тут` на реальний токен

4. Перезавантажте systemd:
```bash
sudo systemctl daemon-reload
```

5. Запустіть сервіс:
```bash
sudo systemctl start telegram-bot
```

6. Перевірте статус:
```bash
sudo systemctl status telegram-bot
```

7. Увімкніть автозапуск:
```bash
sudo systemctl enable telegram-bot
```

**Корисні команди:**
```bash
# Переглянути логи
sudo journalctl -u telegram-bot -f

# Зупинити бота
sudo systemctl stop telegram-bot

# Запустити бота
sudo systemctl start telegram-bot

# Перезапустити бота
sudo systemctl restart telegram-bot
```

---

### Варіант 2: Screen (простий спосіб)

Якщо ви хочете запустити бота в фоновому режимі на вашому комп'ютері.

**Переваги:**
- ✅ Просто налаштувати
- ✅ Можна підключитися до сесії пізніше
- ✅ Працює навіть якщо закрити термінал

**Інструкція:**

1. Запустіть бота в screen:
```bash
cd /home/konko/rust_bot
export TELEGRAM_BOT_TOKEN="ваш_токен_тут"
screen -S telegram-bot cargo run
```

2. Від'єднайтеся від screen (натисніть `Ctrl+A`, потім `D`)

3. Щоб підключитися знову:
```bash
screen -r telegram-bot
```

4. Щоб зупинити бота:
```bash
screen -r telegram-bot
# Натисніть Ctrl+C для зупинки
# Натисніть Ctrl+A, потім K для закриття screen
```

---

### Варіант 3: Tmux (альтернатива screen)

Аналогічно до screen, але з більшими можливостями.

**Інструкція:**

1. Запустіть бота в tmux:
```bash
cd /home/konko/rust_bot
export TELEGRAM_BOT_TOKEN="ваш_токен_тут"
tmux new-session -d -s telegram-bot 'cargo run'
```

2. Підключіться до сесії:
```bash
tmux attach-session -t telegram-bot
```

3. Від'єднайтеся (натисніть `Ctrl+B`, потім `D`)

4. Зупинити бота:
```bash
tmux kill-session -t telegram-bot
```

---

### Варіант 4: VPS/Хмарний сервер (для постійної роботи)

Якщо ви хочете, щоб бот працював навіть коли ваш комп'ютер вимкнений, потрібен VPS (Virtual Private Server).

**Популярні провайдери:**
- DigitalOcean (від $4/місяць)
- Linode (від $5/місяць)
- Vultr (від $2.50/місяць)
- Hetzner (від €4/місяць)
- AWS (Free Tier доступний)
- Google Cloud Platform (Free Tier доступний)

**Кроки:**

1. **Отримайте VPS:**
   - Зареєструйтеся на одному з провайдерів
   - Створіть новий сервер (виберіть Ubuntu або Debian Linux)
   - Отримайте IP адресу та пароль

2. **Підключіться до сервера:**
```bash
ssh root@ваш_ip_адреса
```

3. **Встановіть Rust:**
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source $HOME/.cargo/env
```

4. **Завантажте код бота:**
```bash
# Варіант А: Через git (якщо код на GitHub)
git clone https://github.com/ваш_username/rust_bot.git
cd rust_bot

# Варіант Б: Через scp з вашого комп'ютера
# На вашому комп'ютері:
scp -r /home/konko/rust_bot root@ваш_ip:/root/
```

5. **Налаштуйте токен:**
```bash
export TELEGRAM_BOT_TOKEN="ваш_токен_тут"
```

6. **Скомпілюйте бота:**
```bash
cd rust_bot
cargo build --release
```

7. **Запустіть через systemd** (див. Варіант 1 вище)

---

### Варіант 5: Docker (для просунутих користувачів)

Якщо ви знайомі з Docker, можна запустити бота в контейнері.

**Переваги:**
- ✅ Ізольоване середовище
- ✅ Легко переносити між серверами
- ✅ Автоматичний перезапуск

**Інструкція:**

1. Створіть `Dockerfile`:
```dockerfile
FROM rust:1.75 as builder
WORKDIR /app
COPY . .
RUN cargo build --release

FROM debian:bookworm-slim
RUN apt-get update && apt-get install -y ca-certificates && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY --from=builder /app/target/release/telegram_bot /app/telegram_bot
CMD ["/app/telegram_bot"]
```

2. Створіть `docker-compose.yml`:
```yaml
version: '3.8'
services:
  bot:
    build: .
    restart: always
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
```

3. Запустіть:
```bash
docker-compose up -d
```

---

## 🔍 Перевірка роботи бота

Після запуску будь-яким способом:

1. Відкрийте Telegram
2. Знайдіть вашого бота
3. Надішліть `/start`
4. Перевірте логи (залежить від способу запуску)

---

## ❓ Який варіант обрати?

- **Якщо у вас є сервер, який завжди включений:** Варіант 1 (systemd)
- **Якщо хочете запустити на своєму комп'ютері:** Варіант 2 або 3 (screen/tmux)
- **Якщо хочете, щоб бот працював завжди:** Варіант 4 (VPS)
- **Якщо знайомі з Docker:** Варіант 5

---

## ⚠️ Важливі зауваження

1. **Токен безпеки:** Ніколи не публікуйте токен бота в публічних репозиторіях!
2. **Логи:** Регулярно перевіряйте логи на помилки
3. **Оновлення:** Періодично оновлюйте залежності (`cargo update`)
4. **Резервне копіювання:** Зберігайте копію коду та токена в безпечному місці

---

**Питання?** Перевірте README.md або QUICKSTART.md
