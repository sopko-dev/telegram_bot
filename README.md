# 📚 Покроковий Туторіал: Створення Telegram Бота на Rust

Цей туторіал допоможе вам створити простий Telegram бот на мові Rust з нуля, навіть якщо ви ніколи не програмували раніше!

## 🎯 Що ми будемо робити?

Ми створимо простий бот, який:
- Відповідає на команди `/start`, `/help`, `/info`
- Може бути доданий до вашого Telegram каналу
- Працює без бази даних (просто та зрозуміло)

---

## 📋 Крок 1: Отримання токену бота від Telegram

Перш ніж почати програмувати, нам потрібен токен від Telegram.

### Як отримати токен:

1. **Відкрийте Telegram** на вашому телефоні або комп'ютері
2. **Знайдіть бота @BotFather** в пошуку Telegram
3. **Надішліть команду** `/newbot`
4. **Введіть ім'я бота** (наприклад: "Інформаційний бот для українців")
5. **Введіть username бота** (має закінчуватися на "bot", наприклад: "ukrainians_in_czechia_bot")
6. **BotFather надішле вам токен** - це довгий рядок на кшталт: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`

⚠️ **ВАЖЛИВО:** Збережіть цей токен в безпечному місці! Не діліться ним ніким!

---

## 📋 Крок 2: Встановлення Rust (якщо ще не встановлено)

Якщо Rust вже встановлено, пропустіть цей крок.

### Перевірка, чи встановлено Rust:

Відкрийте термінал (командний рядок) та введіть:
```bash
rustc --version
```

Якщо бачите версію (наприклад, `rustc 1.92.0`), Rust встановлено!

### Якщо Rust не встановлено:

1. Відкрийте сайт: https://www.rust-lang.org/tools/install
2. Завантажте та запустіть інсталятор
3. Дотримуйтесь інструкцій на екрані
4. Після встановлення закрийте та відкрийте термінал знову

---

## 📋 Крок 3: Налаштування проекту

### 3.1. Перейдіть до директорії проекту

Відкрийте термінал та введіть:
```bash
cd /home/konko/rust_bot
```

### 3.2. Перевірте структуру файлів

Ви повинні побачити такі файли:
- `Cargo.toml` - файл з налаштуваннями проекту та залежностями
- `src/main.rs` - головний файл з кодом нашого бота

---

## 📋 Крок 4: Встановлення залежностей

**Залежності** - це готові бібліотеки коду, які ми використовуємо в нашому проекті.

В терміналі введіть:
```bash
cargo build
```

Ця команда:
- Завантажує необхідні бібліотеки з інтернету
- Компілює (перетворює) наш код в програму, яку може виконати комп'ютер

⏳ Це може зайняти кілька хвилин при першому запуску.

---

## 📋 Крок 5: Налаштування токену бота

Тепер нам потрібно сказати програмі, де знайти токен бота.

### Варіант 1: Встановлення змінної оточення (рекомендовано)

В терміналі введіть (замініть `ВАШ_ТОКЕН` на реальний токен від BotFather):
```bash
export TELEGRAM_BOT_TOKEN="ВАШ_ТОКЕН"
```

Наприклад:
```bash
export TELEGRAM_BOT_TOKEN="123456789:ABCdefGHIjklMNOpqrsTUVwxyz"
```

⚠️ **УВАГА:** Ця змінна діє тільки в поточному терміналі. Якщо закриєте термінал, потрібно буде встановити її знову.

### Варіант 2: Створення файлу .env (альтернатива)

Якщо хочете зберігати токен у файлі:

1. Створіть файл `.env` в директорії проекту:
```bash
echo 'TELEGRAM_BOT_TOKEN="ВАШ_ТОКЕН"' > .env
```

2. Потім встановіть змінну:
```bash
export $(cat .env | xargs)
```

---

## 📋 Крок 6: Запуск бота

Тепер все готово! Запустіть бота командою:

```bash
cargo run
```

Ви повинні побачити:
```
Бот запускається...
Бот готовий до роботи! Натисніть Ctrl+C для зупинки.
```

🎉 **Вітаю! Ваш бот працює!**

---

## 📋 Крок 7: Тестування бота

### 7.1. Знайдіть свого бота в Telegram

1. Відкрийте Telegram
2. Знайдіть вашого бота за username (який ви вказали при створенні)
3. Натисніть "Start" або надішліть команду `/start`

### 7.2. Спробуйте команди

Надішліть боту:
- `/start` - почати роботу
- `/help` - отримати допомогу
- `/info` - інформація про канал
- Будь-який інший текст - бот відповість, що не розуміє команду

---

## 📋 Крок 8: Додавання бота до каналу

### 8.1. Зробіть бота адміністратором каналу

1. Відкрийте ваш Telegram канал
2. Перейдіть в налаштування каналу (Settings)
3. Оберіть "Administrators" (Адміністратори)
4. Натисніть "Add Administrator" (Додати адміністратора)
5. Знайдіть вашого бота та додайте його
6. Надайте боту права на відправку повідомлень

### 8.2. Тестування в каналі

Тепер бот може відповідати на команди в каналі!

---

## 🔍 ДУЖЕ ДЕТАЛЬНЕ Пояснення структури коду

### Файл `main.rs` - Повне Пояснення Кожного Рядка

Цей файл містить весь код нашого бота. Кожен рядок має детальні коментарі в самому файлі, але тут ми пояснимо основні концепції:

#### 1. Імпорти (`use teloxide::prelude::*`)

**Що це означає:**
- `use` - це ключове слово Rust для "імпорту" (використання) готових інструментів
- `teloxide` - це назва бібліотеки (як книга з інструкціями)
- `::` - подвійна двокрапка означає "всередині" (як папка в папці)
- `prelude` - це спеціальна папка з найбільш використовуваними речами
- `*` - зірочка означає "все" (імпортуємо все з цієї папки)

**Навіщо це потрібно:**
Без цього рядка ми не змогли б використовувати типи `Bot`, `Message`, `Dispatcher` тощо.

#### 2. Атрибут `#[tokio::main]`

**Що це означає:**
- `#` - символ для атрибутів (спеціальних інструкцій для компілятора)
- `[ ]` - квадратні дужки обгортають атрибут
- `tokio` - назва бібліотеки для асинхронного програмування
- `::main` - конкретний атрибут з tokio

**Навіщо це потрібно:**
Цей атрибут автоматично перетворює нашу функцію `main()` в асинхронну. Без нього Rust не дозволить нам використовувати `async fn main()`.

#### 3. Функція `main()` - Головна Функція

**Що це означає:**
- `async` - означає "асинхронна" (може чекати на операції)
- `fn` - означає "function" (функція)
- `main` - завжди назва головної функції в Rust
- `()` - порожні дужки означають "без параметрів"
- `{ }` - фігурні дужки обгортають код функції

**Що вона робить:**
1. Виводить повідомлення "Бот запускається..."
2. Отримує токен зі змінної оточення
3. Створює об'єкт бота
4. Створює диспетчер (який обробляє повідомлення)
5. Запускає диспетчер (бот починає слухати повідомлення)

#### 4. Отримання токену (`std::env::var`)

**Що це означає:**
- `std` - стандартна бібліотека Rust
- `env` - модуль для роботи зі змінними оточення
- `var` - функція для отримання значення змінної
- `.expect()` - якщо змінна не знайдена, програма зупиниться з повідомленням

**Приклад:**
Якщо ви встановили `export TELEGRAM_BOT_TOKEN="123:ABC"`, то `std::env::var("TELEGRAM_BOT_TOKEN")` поверне `"123:ABC"`.

#### 5. Створення бота (`Bot::new`)

**Що це означає:**
- `Bot` - це тип (як шаблон) з бібліотеки teloxide
- `::new` - метод для створення нового об'єкта
- `&bot_token` - передаємо посилання на токен (економимо пам'ять)

**Результат:**
Створюється об'єкт `bot`, який представляє нашого бота в програмі.

#### 6. Диспетчер (`Dispatcher::builder`)

**Що це означає:**
- `Dispatcher` - це тип, який відповідає за обробку повідомлень
- `::builder` - метод для створення диспетчера крок за кроком
- `Update::filter_message()` - фільтрує тільки текстові повідомлення
- `.endpoint(handle_message)` - вказує функцію, яка обробляє повідомлення

**Що він робить:**
Диспетчер постійно перевіряє, чи є нові повідомлення від Telegram. Якщо є - викликає функцію `handle_message`.

#### 7. Функція `handle_message()` - Обробка Повідомлень

**Параметри:**
- `bot: Bot` - наш об'єкт бота (для відправки повідомлень)
- `msg: Message` - повідомлення від користувача

**Що вона робить:**
1. Перевіряє, чи є текст у повідомленні (`if let Some(text) = msg.text()`)
2. Використовує `match` для перевірки команди:
   - Якщо `/start` - відправляє привітання
   - Якщо `/help` - відправляє допомогу
   - Якщо `/info` - відправляє інформацію про канал
   - Якщо щось інше - відправляє повідомлення про невідому команду
3. Повертає `Ok(())` - означає "успішно виконано"

#### 8. Конструкція `match` - Перевірка Варіантів

**Що це означає:**
- `match` - ключове слово для перевірки різних варіантів
- `"/start" => { ... }` - якщо текст дорівнює "/start", виконати код у фігурних дужках
- `_ => { ... }` - підкреслення означає "будь-який інший варіант"

**Приклад:**
```rust
match text {
    "/start" => { /* код для /start */ }
    "/help" => { /* код для /help */ }
    _ => { /* код для інших команд */ }
}
```

#### 9. Відправка повідомлення (`bot.send_message`)

**Що це означає:**
- `bot` - наш об'єкт бота
- `.send_message` - метод для відправки повідомлення
- `msg.chat.id` - ID чату (куди відправити)
- `"Текст"` - текст повідомлення
- `.await?` - чекаємо на відправку та обробляємо помилки

**Приклад:**
```rust
bot.send_message(msg.chat.id, "Привіт!").await?;
```

#### 10. Повернення результату (`Ok(())`)

**Що це означає:**
- `Ok` - означає "успішно"
- `()` - порожні дужки означають "нічого" (функція виконалася, але не повертає значення)

**Навіщо це потрібно:**
Функція має повернути `ResponseResult<()>`, тому ми повертаємо `Ok(())`.

### Файл `Cargo.toml` - Детальне Пояснення

Цей файл описує наш проект. Давайте розберемо кожен рядок:

#### `[package]` - Секція Проекту
- `[package]` - квадратні дужки означають "секція"
- `package` - означає "пакет" (наш проект)

#### `name = "telegram_bot"`
- `name` - назва проекту
- `=` - знак рівності означає "дорівнює"
- `"telegram_bot"` - назва нашого проекту в лапках

#### `version = "0.1.0"`
- `version` - версія проекту
- `"0.1.0"` - формат версії: основна.другорядна.патч

#### `edition = "2021"`
- `edition` - версія мови Rust
- `"2021"` - використовуємо версію 2021

#### `[dependencies]` - Секція Залежностей
- `[dependencies]` - секція з залежностями (бібліотеками)
- `dependencies` - означає "залежності" (інші бібліотеки, які нам потрібні)

#### `teloxide = "0.17"`
- `teloxide` - назва бібліотеки для роботи з Telegram
- `=` - знак рівності
- `"0.17"` - версія бібліотеки

#### `tokio = { version = "1", features = ["rt-multi-thread", "macros"] }`
- `tokio` - назва бібліотеки для асинхронного програмування
- `=` - знак рівності
- `{ }` - фігурні дужки означають "об'єкт з налаштуваннями"
- `version = "1"` - версія бібліотеки
- `features = [...]` - додаткові можливості бібліотеки
- `"rt-multi-thread"` - можливість використовувати кілька потоків
- `"macros"` - можливість використовувати макроси (як `#[tokio::main]`)

**Навіщо це потрібно:**
Cargo (менеджер пакетів Rust) читає цей файл і автоматично завантажує всі необхідні бібліотеки з інтернету.

---

## 🛠️ Корисні команди

### Компіляція проекту (без запуску):
```bash
cargo build
```

### Запуск проекту:
```bash
cargo run
```

### Перевірка коду на помилки:
```bash
cargo check
```

### Очищення скомпільованих файлів:
```bash
cargo clean
```

---

## ❓ Часті питання

### Бот не запускається, помилка "TELEGRAM_BOT_TOKEN не знайдено"

**Рішення:** Переконайтеся, що ви встановили змінну оточення:
```bash
export TELEGRAM_BOT_TOKEN="ваш_токен"
```

### Бот не відповідає на команди

**Можливі причини:**
1. Перевірте, що бот запущений (ви бачите "Бот готовий до роботи!")
2. Перевірте, що ви надсилаєте команди правильному боту
3. Перезапустіть бота (Ctrl+C, потім `cargo run` знову)

### Як зупинити бота?

Натисніть `Ctrl+C` в терміналі, де запущений бот.

---

## 🎓 Наступні кроки для навчання

Після того, як ви зрозуміли базовий бот, можете спробувати:

1. **Додати нові команди** - додайте більше команд у функцію `handle_message()`
2. **Змінити тексти відповідей** - змініть повідомлення, які відправляє бот
3. **Додати обробку різних типів повідомлень** - не тільки текст, але й фото, відео тощо
4. **Вивчити більше про Rust** - читайте документацію на https://doc.rust-lang.org/book/

---

## 📞 Допомога

Якщо у вас виникли проблеми:
1. Перевірте, чи всі кроки виконані правильно
2. Переконайтеся, що токен правильний
3. Перевірте, що Rust встановлено правильно
4. Перезапустіть термінал та спробуйте знову

---

**Успіхів у навчанні Rust! 🦀**
