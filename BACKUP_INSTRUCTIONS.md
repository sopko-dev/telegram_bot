# 💾 Інструкція: Створення резервної копії на GitHub

## ✅ Що вже зроблено:

1. ✅ Всі файли додані до git
2. ✅ Створено commit з резервною копією
3. ✅ Налаштовано remote на GitHub
4. ✅ Готово до завантаження

## 📤 Як завантажити резервну копію:

### Варіант 1: Через GitHub CLI (найпростіший)

```bash
cd /home/konko/rust_bot

# 1. Авторизуйтеся (відкриється браузер)
gh auth login

# 2. Завантажте код
git push origin main
```

### Варіант 2: Через Personal Access Token

1. **Створіть токен на GitHub:**
   - Відкрийте: https://github.com/settings/tokens
   - Натисніть "Generate new token" → "Generate new token (classic)"
   - Назвіть: "rust_bot_backup"
   - Оберіть права: `repo` (повний доступ)
   - Натисніть "Generate token"
   - **Скопіюйте токен!**

2. **Завантажте код:**
   ```bash
   cd /home/konko/rust_bot
   git push https://ВАШ_ТОКЕН@github.com/sopko-dev/telegram_bot.git main
   ```

   Або введіть токен як пароль:
   ```bash
   git push origin main
   # Username: sopko-dev
   # Password: вставте ваш токен
   ```

### Варіант 3: Через SSH

1. **Створіть SSH ключ (якщо немає):**
   ```bash
   ssh-keygen -t ed25519 -C "ваш_email@example.com"
   # Натисніть Enter для всіх питань
   ```

2. **Скопіюйте публічний ключ:**
   ```bash
   cat ~/.ssh/id_ed25519.pub
   ```

3. **Додайте ключ на GitHub:**
   - Відкрийте: https://github.com/settings/keys
   - Натисніть "New SSH key"
   - Вставте ключ
   - Натисніть "Add SSH key"

4. **Змініть remote на SSH:**
   ```bash
   cd /home/konko/rust_bot
   git remote set-url origin git@github.com:sopko-dev/telegram_bot.git
   ```

5. **Завантажте код:**
   ```bash
   git push origin main
   ```

## 🚀 Швидкий спосіб (використовуючи скрипт):

```bash
cd /home/konko/rust_bot
./backup_to_github.sh
```

Скрипт автоматично:
- Додасть всі файли
- Створить commit
- Покаже інструкції для push

## 📋 Що буде завантажено:

- ✅ Весь код бота (`src/main.rs`)
- ✅ Всі документації (README, QUICKSTART, FLY_IO_GUIDE, тощо)
- ✅ Конфігурації для деплою (fly.toml, railway.json, render.yaml)
- ✅ Скрипти для запуску та резервного копіювання
- ✅ Всі інструкції та гайди

## 🔍 Перевірка після завантаження:

Відкрийте https://github.com/sopko-dev/telegram_bot і перевірте:
- ✅ Всі файли присутні
- ✅ Останній commit відображається
- ✅ Код можна переглянути

## 💡 Поради:

1. **Регулярно робіть резервні копії:**
   ```bash
   ./backup_to_github.sh
   ```

2. **Перевіряйте статус:**
   ```bash
   git status
   git log --oneline -5
   ```

3. **Якщо щось пішло не так:**
   ```bash
   git log --oneline
   git show HEAD  # Переглянути останній commit
   ```

---

**Готово!** Після виконання однієї з команд вище, ваша резервна копія буде на GitHub! 🎉
