# ⚠️ Проблема з токеном - Рішення

## Помилка: Permission denied (403)

Це означає, що токен не має достатніх прав або репозиторій недоступний.

## 🔧 Рішення:

### 1. Перевірте права токена:

1. Відкрийте: https://github.com/settings/tokens
2. Знайдіть ваш токен
3. Перевірте, чи встановлено право **`repo`** (повний контроль над репозиторіями)
4. Якщо немає - створіть новий токен з правом `repo`

### 2. Перевірте доступ до репозиторію:

Переконайтеся, що:
- Репозиторій існує: https://github.com/sopko-dev/telegram_bot
- Ви маєте права на запис в репозиторій
- Репозиторій не приватний (або токен має доступ до приватних репозиторіїв)

### 3. Створіть новий токен з правильними правами:

1. Відкрийте: https://github.com/settings/tokens
2. Відкликайте старий токен (Revoke)
3. Створіть новий:
   - **Note:** `rust_bot_backup_v2`
   - **Expiration:** `No expiration`
   - **Scopes:** ✅ **`repo`** (обов'язково!)
4. Скопіюйте новий токен
5. Використайте:

```bash
cd /home/konko/rust_bot
git push https://НОВИЙ_ТОКЕН@github.com/sopko-dev/telegram_bot.git main
```

### 4. Альтернатива: Використайте GitHub CLI

```bash
# Авторизуйтеся
gh auth login

# Завантажте код
cd /home/konko/rust_bot
git push origin main
```

---

## ⚠️ ВАЖЛИВО: Безпека токена

**Ваш токен був опублікований в чаті!**

Після успішного завантаження:
1. Відкрийте: https://github.com/settings/tokens
2. Знайдіть токен `github_pat_11B5EGWPQ06...`
3. Натисніть **"Revoke"** (відкликати)
4. Створіть новий токен для майбутнього використання

---

## ✅ Перевірка після виправлення:

```bash
cd /home/konko/rust_bot
git push origin main
```

Якщо все добре, ви побачите:
```
Enumerating objects: ...
Writing objects: ...
To https://github.com/sopko-dev/telegram_bot.git
   aa92e48..56ec232  main -> main
```
