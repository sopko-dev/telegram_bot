# 🚀 Швидкий старт

## Крок 1: Отримайте токен бота

1. Відкрийте Telegram
2. Знайдіть @BotFather
3. Надішліть `/newbot`
4. Дотримуйтесь інструкцій
5. Скопіюйте токен (виглядає як `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Крок 2: Встановіть токен

**⚠️ ЦЕ ОБОВ'ЯЗКОВО! Без токена бот не працюватиме!**

В терміналі виконайте (замініть `ваш_токен_тут` на реальний токен від BotFather):

```bash
export TELEGRAM_BOT_TOKEN="ваш_токен_тут"
```

**Приклад:**
```bash
export TELEGRAM_BOT_TOKEN="123456789:ABCdefGHIjklMNOpqrsTUVwxyz"
```

**Перевірка, чи токен встановлено:**
```bash
echo $TELEGRAM_BOT_TOKEN
```

Якщо бачите токен - все добре! Якщо порожньо - токен не встановлено.

**⚠️ УВАГА:** Змінна `export` діє тільки в поточному терміналі. Якщо закриєте термінал, потрібно буде встановити її знову.

## Крок 3: Запустіть бота

**ВАЖЛИВО:** Переконайтеся, що токен встановлено (Крок 2)!

### Варіант 1: Використання скрипта (рекомендовано)

```bash
cd /home/konko/rust_bot
./run_bot.sh
```

Скрипт автоматично перевірить, чи встановлено токен.

### Варіант 2: Прямий запуск

```bash
cd /home/konko/rust_bot
cargo run
```

**Якщо бачите помилку "TELEGRAM_BOT_TOKEN не знайдено":**
- ❌ Токен не встановлено! Поверніться до Кроку 2
- Переконайтеся, що ви виконали `export TELEGRAM_BOT_TOKEN="..."` в тому самому терміналі
- Або використайте скрипт `./run_bot.sh` - він покаже, що не так

Ви повинні побачити:
```
Бот запускається...
Бот готовий до роботи! Натисніть Ctrl+C для зупинки.
```

## Крок 4: Протестуйте бота

1. Відкрийте Telegram
2. Знайдіть вашого бота (за username, який ви вказали)
3. Надішліть `/start`
4. Бот має відповісти привітанням!

## Доступні команди

- `/start` - почати роботу з ботом
- `/help` - отримати допомогу
- `/info` - інформація про канал

## 💬 Відповіді на питання

Бот може відповідати на різні питання! Просто напишіть боту своє питання (без команди `/`), і він постарається допомогти.

**Теми, на які бот може відповісти:**
- 📄 Документи та візи
- 💼 Робота та працевлаштування
- 🎓 Навчання та освіта
- 🏥 Медицина та здоров'я
- 🏠 Житло та оренда
- 📚 Чеська мова та курси
- 💰 Фінанси та банки
- 🚌 Транспорт
- 🇨🇿 Загальна інформація про Чехію
- 🤝 Допомога та підтримка

**Приклади питань:**
- "Як отримати документи?"
- "Де знайти роботу?"
- "Як вивчати чеську мову?"
- "Де можна отримати медичну допомогу?"
- "Як відкрити банківський рахунок?"
- "Як користуватися транспортом?"

Просто напишіть питання боту, і він відповість! 😊

## 💬 Обробка питань

Бот тепер може відповідати на різні питання! Просто напишіть боту своє питання (не команду), і він постарається допомогти.

**Приклади питань:**
- "Привіт" / "Добрий день"
- "Як отримати документи?"
- "Де знайти роботу?"
- "Як вивчати чеську мову?"
- "Де отримати медичну допомогу?"
- "Як знайти житло?"
- "Що таке Чехія?"

Бот розуміє питання про:
- 📄 Документи та візи
- 💼 Роботу та працевлаштування
- 🎓 Навчання та освіту
- 🏥 Медицину та здоров'я
- 🏠 Житло
- 📚 Чеську мову
- 🇨🇿 Загальну інформацію про Чехію

## Зупинка бота

Натисніть `Ctrl+C` в терміналі, де запущений бот.

## Додавання бота до каналу

**ВАЖЛИВО:** Бот тепер підтримує канали! Але потрібно правильно налаштувати:

1. Відкрийте ваш Telegram канал
2. Натисніть на назву каналу (вгорі)
3. Оберіть **"Адміністратори"** (Administrators)
4. Натисніть **"Додати адміністратора"** (Add Administrator)
5. Знайдіть вашого бота та додайте його
6. **ОБОВ'ЯЗКОВО:** Надайте боту права на **"Публікувати повідомлення"** (Post messages)
7. Збережіть налаштування

**Після цього бот зможе відповідати на команди в каналі!**

📖 **Детальні інструкції:** Дивіться файл `KANAL_INSTRUKCIYA.md`

---

## ❌ Розв'язання проблем

### Проблема: Бот не запускається, помилка "TELEGRAM_BOT_TOKEN не знайдено"

**Причина:** Токен не встановлено або встановлено в іншому терміналі.

**Рішення:**
1. Перевірте, чи токен встановлено:
   ```bash
   echo $TELEGRAM_BOT_TOKEN
   ```
2. Якщо порожньо - встановіть токен:
   ```bash
   export TELEGRAM_BOT_TOKEN="ваш_токен_тут"
   ```
3. Запустіть бота знову в тому самому терміналі

### Проблема: Бот запускається, але не відповідає на команди

**Можливі причини:**
1. Бот не запущений (перевірте термінал - має бути "Бот готовий до роботи!")
2. Ви надсилаєте команди неправильному боту
3. Для каналів: бот не доданий як адміністратор

**Рішення:**
1. Перевірте, чи бот запущений (в терміналі має бути повідомлення)
2. Перевірте логи в терміналі - бот виводить всі отримані повідомлення
3. Для каналів: додайте бота як адміністратора з правами на публікацію

### Проблема: Команди не розпізнаються

**Перевірте:**
- Команда починається з `/`? (наприклад `/start`, а не `start`)
- Ви надсилаєте команду в правильному чаті/каналі?

**Рішення:**
- Використовуйте формат `/start`, `/help`, `/info`
- Перевірте логи в терміналі - там видно, що отримує бот

---

## 🔄 Запуск бота в фоновому режимі

**Якщо ви хочете, щоб бот працював навіть після закриття терміналу:**

### Варіант 1: Використання screen (простий спосіб)

```bash
cd /home/konko/rust_bot
export TELEGRAM_BOT_TOKEN="ваш_токен_тут"
./run_background.sh
```

Або вручну:
```bash
screen -S telegram-bot
export TELEGRAM_BOT_TOKEN="ваш_токен_тут"
cargo run
# Натисніть Ctrl+A, потім D для від'єднання
```

### Варіант 2: Systemd service (для серверів)

Якщо у вас є сервер, який завжди включений:

```bash
cd /home/konko/rust_bot
export TELEGRAM_BOT_TOKEN="ваш_токен_тут"
sudo ./install_service.sh
```

**⚠️ Важливо:** Якщо ви вимкнете комп'ютер, бот перестане працювати. Для постійної роботи потрібен VPS або сервер.

💰 **Шукаєте безкоштовний варіант?** Дивіться файл `FREE_DEPLOYMENT.md` - там є кілька повністю безкоштовних хмарних сервісів!

📖 **Детальна інструкція для Fly.io:** Дивіться файл `FLY_IO_GUIDE.md` - повна покрокова інструкція з усіма деталями!

📖 **Детальні інструкції:** Дивіться файл `DEPLOYMENT.md`

---

**Проблеми?** Перевірте README.md для детальних інструкцій або DIAGNOSTIKA.md для діагностики.
